import React from "react";
import theme from "../../theme";

function Footer() {
return (
    <footer style={{ position: "fixed", bottom: 0, width: "100%", background: theme.palette.gradient.main }}>
            <p>its kinda ugly...</p>
    </footer>
);
}

export default Footer;